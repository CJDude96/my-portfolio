import React, {FC} from "react"
import Image from "next/image"
import Tooltip from "./Tooltip"

interface SkillCardProps {
    tech: string,
    text: string
}

const SkillCard: FC<SkillCardProps> = ({ tech, text }) => {
  return (
    <div>
        
    </div>
  )
}

export default SkillCard